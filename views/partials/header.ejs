<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Memes App</title>
  <link rel="icon" href="/images/favicon.png" type="image/png">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body>

  <nav class="navbar navbar-expand-sm bg-light px-3 py-2 mb-4">
    <div class="container-fluid d-flex justify-content-between align-items-center">

      <!-- Left side links (always visible) -->
      <div class="d-flex gap-3 align-items-center">
        <a class="navbar-brand" href="/">🏠 Home</a>
        <a class="navbar-brand" href="/memes">All Memes</a>
        <a class="navbar-brand" href="/memes/new">Add Meme</a>
      </div>

      <!-- Right side (changes based on auth state) -->
      <div class="d-flex gap-3 align-items-center">
        <% if (currentUser) { %>
          <a class="navbar-brand" href="/memes/mine">My Memes</a>
          <a class="navbar-brand" href="/users/community">Community Members</a>
          <a class="navbar-brand" href="/users/<%= currentUser._id %>">View/Edit Profile</a>
          <form action="/logout" method="POST" style="display:inline;">
            <button type="submit" class="btn btn-outline-danger btn-sm">Log Out</button>
          </form>
          <span class="text-muted small">Signed in as <strong><%= currentUser.name %></strong></span>
        <% } else { %>
          <a class="navbar-brand" href="/login">Log In</a>
          <a class="navbar-brand" href="/register">Register</a>
        <% } %>
      </div>

    </div>
  </nav>  

  <main class="container" style="max-width: 900px;">

