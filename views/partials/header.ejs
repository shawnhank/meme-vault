<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Memes App</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body>
  <nav class="navbar navbar-expand-sm bg-light px-3 py-2 mb-4">
    <div class="container-fluid d-flex justify-content-between align-items-center">

      <!-- Left side nav links -->
      <div class="d-flex gap-3 align-items-center">
        <a class="navbar-brand" href="/">🏠 Home</a>
        <a class="navbar-brand" href="/memes">All Memes</a>
        <a class="navbar-brand" href="/memes/new">Add Meme</a>

        <% if (currentUser) { %>
          <a class="navbar-brand" href="/users/<%= currentUser._id %>">My Profile</a>
          <a class="navbar-brand" href="/users/<%= currentUser._id %>/edit">Edit</a>
          <a class="navbar-brand" href="/users/<%= currentUser._id %>/change-password">Password</a>
          <form action="/logout" method="POST" style="display:inline;">
            <button type="submit" class="btn btn-outline-danger btn-sm">Log Out</button>
          </form>
        <% } else { %>
          <a class="navbar-brand" href="/login">Log In</a>
          <a class="navbar-brand" href="/register">Register</a>
        <% } %>
      </div>

      <!-- Right side: show user name if signed in -->
      <% if (currentUser) { %>
        <div class="text-muted small">
          Signed in as <strong><%= currentUser.name %></strong>
        </div>
      <% } %>

    </div>
  </nav>

  <%- include('../partials/flash') %>

  <main class="container" style="max-width: 900px;">

