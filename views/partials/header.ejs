<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Memes App</title>
  <link rel="icon" href="/images/favicon.png" type="image/png">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
  <link rel="stylesheet" href="/css/main.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body data-bs-theme="light">

  <header>
    <nav class="navbar navbar-expand-sm bg-body-tertiary px-4 py-4 mb-4 w-100">
      <div class="container-fluid">

        <!-- Mobile toggle -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-between align-items-center" id="navbarResponsive">

          <!-- Left section -->
          <div class="d-flex flex-wrap gap-2 align-items-center">
            <a href="/" class="nav-link d-flex gap-3 align-items-center fs-5 fw-semibold">
              <img src="/images/logo3b.png" alt="Mem Vault logo" style="height: 32px;">
              <span>Meme Vault</span>
            </a>
          </div>

          <!-- Right section -->
          <div class="d-flex flex-wrap gap-4 align-items-center">
            <% if (currentUser) { %>
              <a class="nav-link fs-5" href="/users/community">Community</a>
            <% } %>
            <div class="nav-item position-relative">
              <a class="nav-link fs-5" href="#">Memes</a>
              <div class="position-absolute bg-dark submenu py-2;" style="min-width: 150px;">
                <a href="/memes/new" class="d-block text-light text-decoration-none py-1">Add a Meme</a>
                <a href="/memes" class="d-block text-light text-decoration-none py-1">Browse Memes</a>  
                <% if (currentUser) { %>
                  <a href="/memes/mine" class="d-block text-light text-decoration-none py-2">My Memes</a>
                  <a href="/users/<%= currentUser._id %>/favorites" class="d-block text-light text-decoration-none py-2">My Favorites</a>
                <% } %>         
              </div>
            </div>
            <% if (currentUser) { %>
              <div class="nav-item position-relative">
                <span class="nav-link fs-5"><%= currentUser.name %></span>
                <div class="submenu bg-dark rounded py-2" style="min-width: 150px;">
                  <a href="/users/<%= currentUser._id %>" class="d-block text-light text-decoration-none px-3 py-2">Edit Profile</a>
                  <a href="/users/<%= currentUser._id %>/change-password" class="d-block text-light text-decoration-none px-3 py-2">Change Password</a>
                  <form action="/logout" method="POST" class="m-0">
                    <button type="submit" class="logout-link">Log Out</button>
                  </form>
                </div>
              </div>
            <% } else { %>
              <div class="nav-item position-relative">
                <a class="nav-link fs-5" href="/login">Log In</a>
                  <div class="position-absolute bg-dark submenu py-2;" style="min-width: 150px;">
                    <a class="nav-link fs-5" href="/register">Create Account</a>
                  </div>
              </div>    
            <% } %>

            <button id="theme-toggle" class="btn btn-sm" aria-label="Toggle theme" data-bs-toggle="tooltip" title="Toggle Dark Mode">
              <i id="theme-icon" class="fas fa-moon fs-5"></i>
            </button>
          </div>
        </div>
      </div>
    </nav>
  </header>

