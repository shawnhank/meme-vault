<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Memes App</title>
  <link rel="icon" href="/images/favicon.png" type="image/png">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
  <link rel="stylesheet" href="/css/main.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body data-bs-theme="dark">

  <header>
    <nav class="navbar navbar-expand-sm bg-body-tertiary px-4 py-4 mb-4 w-100">
      <div class="container-fluid">

        <!-- Mobile toggle -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-between align-items-center" id="navbarResponsive">

          <!-- Left section -->
          <div class="d-flex flex-wrap gap-4 align-items-center">
            <a href="/" class="nav-link d-flex gap-2 align-items-center fs-5 fw-semibold">
              <img src="/images/logo3b.png" alt="Meme Vault logo" style="height: 32px;">
              <span>Meme Vault</span>
            </a>
            <a class="nav-link fs-5" href="/memes">Browse Memes</a>
            <a class="nav-link fs-5" href="/memes/new">Add Meme</a>
          </div>

          <!-- Right section -->
          <div class="d-flex flex-wrap gap-4 align-items-center">

            <% if (currentUser) { %>
              <a class="nav-link fs-5" href="/memes/mine">My Memes</a>
              <a class="nav-link fs-5" href="/users/community">Community</a>
              <a class="nav-link fs-5" href="/users/<%= currentUser._id %>" title="View/Edit Profile">
                <%= currentUser.name %>
              </a>
              <form action="/logout" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-outline-danger btn-sm">Log Out</button>
              </form>
            <% } else { %>
              <a class="nav-link fs-5" href="/login">Log In</a>
              <a class="nav-link fs-5" href="/register">Register</a>
            <% } %>

            <button id="theme-toggle" class="btn btn-sm" aria-label="Toggle theme" data-bs-toggle="tooltip" title="Toggle Dark Mode">
              <i id="theme-icon" class="fas fa-moon fs-5"></i>
            </button>
          </div>
        </div>
      </div>
    </nav>
  </header>

