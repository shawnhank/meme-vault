<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Memes App</title>
  <link rel="icon" href="/images/favicon.png" type="image/png">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
  <link rel="stylesheet" href="/css/main.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body data-bs-theme="dark">

  <nav class="navbar navbar-expand-sm bg-body-tertiary px-3 py-2 mb-4">
  <div class="container-fluid">

    <!-- Brand + Toggle button (mobile) -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-between align-items-center" id="navbarResponsive">

      <!-- Left side links -->
      <!-- Left side links wrapped for consistent spacing -->
      <div class="d-flex flex-wrap gap-3 align-items-center">
        <a href="/" class="nav-link d-flex gap-2 align-items-center">
          <img src="/images/logo3b.png" alt="Meme Vault logo" style="height: 24px;">
          <span class="fw-semibold">Home</span>
        </a>
        <a class="nav-link" href="/memes">All Memes</a>
        <a class="nav-link" href="/memes/new">Add Meme</a>
      </div>
      
      <!-- Right side -->
      <div class="d-flex flex-wrap gap-3 align-items-center">

        <% if (currentUser) { %>
          <a class="nav-link" href="/memes/mine">My Memes</a>
          <a class="nav-link" href="/users/community">Community</a>
          <a class="nav-link" href="/users/<%= currentUser._id %>" title="View/Edit Profile">
            <%= currentUser.name %>
          </a>
        <% } else { %>
          <a class="nav-link" href="/login">Log In</a>
          <a class="nav-link" href="/register">Register</a>
        <% } %>
        <form action="/logout" method="POST" style="display:inline;">
          <button type="submit" class="btn btn-outline-danger btn-sm">Log Out</button>
        </form>  
        <button id="theme-toggle" class="btn btn-sm" aria-label="Toggle theme" data-bs-toggle="tooltip" title="Toggle Dark Mode">
          <i id="theme-icon" class="fas fa-moon"></i>
        </button>
      </div>
    </div>
  </div>
</nav>

